{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Всё, что окну разрешено. Один пункт сверх обязательного: диалог выбора папки. Бэкенд запускается из Rust, поэтому право запускать процессы окну не выдаётся — страница, которая смогла бы запустить что угодно, была бы дырой, а не удобством.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
