{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Всё, что окну разрешено. Сверх обязательного: диалог выбора папки, проверка обновлений и перезапуск в новую версию. Бэкенд запускается из Rust, поэтому право запускать процессы окну не выдаётся — страница, которая смогла бы запустить что угодно, была бы дырой, а не удобством.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "updater:default",
    "process:allow-restart"
  ]
}
